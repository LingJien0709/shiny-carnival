[phases.setup]
nixPkgs = ["nodejs_18", "npm"]

[phases.install]
cmds = ["cd backend && npm install && npm run db:generate"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "cd backend && npm start"



